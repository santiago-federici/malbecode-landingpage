---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<button
  class={cn(
    "hover:bg-primary/60 bg-primary rounded-sm font-medium cursor-pointer duration-100 px-4 py-2",
    className,
  )}
>
  <slot />
</button>
